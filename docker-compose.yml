services:
  frontend:
<<<<<<< Updated upstream
    build: ./client
=======
    build:
      context: ./client
      args:
        - REACT_APP_API_URL=http://localhost:5000
        - REACT_APP_WS_URL=ws://localhost:5000/api/ws
>>>>>>> Stashed changes
    container_name: timecell_frontend
    ports:
      - "80:3000"
    environment:
      - HOST=0.0.0.0
    depends_on:
      - backend
    stdin_open: true
    tty: true

  backend:
    build: ./server
    container_name: timecell_backend
    ports:
      - "5000:5000"
    environment:
      - DB_HOST=db
      - DB_USER=root
      - DB_PASSWORD=${MYSQL_ROOT_PASSWORD}
      - DB_NAME=TimeCell_db
<<<<<<< Updated upstream
=======
      - CLIENT_URL=http://localhost:3000
      - AI_URL=http://ai:8000
      - AI_WS_URL=ws://ai:8000/ws

>>>>>>> Stashed changes
    depends_on:
      - db
    restart: always

  db:
    image: mysql:8
    container_name: timecell_db
    ports:
      - "3307:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: TimeCell_db
    volumes:
      - timecell_data:/var/lib/mysql
      - ./server/db/init.sql:/docker-entrypoint-initdb.d/init.sql
    restart: always

volumes:
  timecell_data: